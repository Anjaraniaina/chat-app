import React from 'react';
import Protected from "@/components/Protected";

const ChannelPage = () => {
  return (
    <Protected>
        <div>
            Channel Page
        </div>
    </Protected>
  );
};

export default ChannelPage;